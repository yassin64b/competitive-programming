/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>
#include <sstream>
#include <unordered_set>

using namespace std;

class aoc4_2 {
public:
    void solve(istream& in, ostream& out) {
        string passw;
        int res = 0;
        while (getline(in, passw)) {
            stringstream ss(passw);
            unordered_set<string> seen;
            bool ok = true;
            string s;
            while (ss >> s) {
                sort(s.begin(), s.end());
                if (seen.find(s) != seen.end())
                    ok = false;
                seen.insert(s);
            }
            res += ok;
        }
        out << res << "\n";
    }
};


int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);

    aoc4_2 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
