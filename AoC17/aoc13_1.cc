/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>
#include <sstream>

using namespace std;

class aoc13_1 {
public:
    void solve(istream& in, ostream& out) {
        string s;
        vector<pair<int, int>> firewall;
        while (getline(in, s)) {
            stringstream ss(s);
            char trash;
            int depth, range;
            ss >> depth >> trash >> range;
            firewall.emplace_back(depth, range);
        }
        int res = 0;
        for (int i = 0; i < (int)firewall.size(); ++i) {
            int guard = firewall[i].first % (2 * firewall[i].second - 2);
            if (guard == 0)
                res += firewall[i].first * firewall[i].second;
        }
        out << res << "\n";
    }
};


int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);

    aoc13_1 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
