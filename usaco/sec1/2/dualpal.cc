/*
ID: yassin.2
PROG: dualpal
LANG: C++11
*/

/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin64b
 */

#include <fstream>

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>

using namespace std;

class dualpal {
private:
    string toBase(int x, int b) {
        string res;
        while (x) {
            int tmp = x % b;
            x /= b;
            if (tmp <= 9) {
                res += tmp + '0';
            } else {
                res += tmp - 10 + 'A';
            }
        }
        reverse(res.begin(), res.end());
        return res;
    }
    
    bool isPalindrome(string s) {
        size_t n = s.size();
        for (size_t i = 0; i < n / 2; ++i) {
            if (s[i] != s[n - i - 1]) {
                return false;
            }
        }
        return true;
    }
public:
    void solve(istream& in, ostream& out) {
        int N, S;
        in >> N >> S;

        int found = 0;
        for (++S; found < N; ++S) {
            int cnt = 0;
            for (int b = 2; cnt < 2 && b <= 10; ++b) {
                cnt += isPalindrome(toBase(S, b));
            }
            if (cnt == 2) {
                out << S << endl;
                ++found;
            }
        }
    }
};


int main() {
    std::ios::sync_with_stdio(false);

    dualpal solver;
    std::ifstream in("dualpal.in");
    std::ofstream out("dualpal.out");
    solver.solve(in, out);
    return 0;
}
