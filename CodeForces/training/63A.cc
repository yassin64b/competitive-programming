/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>
#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>
#include <sstream>

using namespace std;

class A63 {
public:
    void solve(istream& in, ostream& out) {
        int n;
        in >> n;
        vector<pair<string, string>> crew(n);
        for (int i = 0; i < n; ++i)
            in >> crew[i].first >> crew[i].second;
        for (int i = 0; i < n; ++i)
            if (crew[i].second == "rat") out << crew[i].first << "\n";
        for (int i = 0; i < n; ++i)
            if (crew[i].second == "woman" || crew[i].second == "child")
                out << crew[i].first << "\n";
        for (int i = 0; i < n; ++i)
            if (crew[i].second == "man") out << crew[i].first << "\n";
        for (int i = 0; i < n; ++i)
            if (crew[i].second == "captain") out << crew[i].first << "\n";
    }
};


int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);
    A63 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
