/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>
#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>
#include <sstream>

using namespace std;

class A312 {
public:
    void solve(istream& in, ostream& out) {
        int n;
        in >> n;
        in.ignore();
        for (int i = 0; i < n; ++i) {
            string s;
            getline(in, s);
            if (s.size() >= 5U && s.substr(0, 5) == "miao." && !(s.substr(s.size() - 5) == "lala."))
                out << "Rainbow's\n";
            else if (s.size() >= 5U && s.substr(s.size() - 5) == "lala." && !(s.substr(0, 5) == "miao."))
                out << "Freda's\n";
            else
                out << "OMG>.< I don't know!\n";
        }
    }
};


int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);
    A312 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
