/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <cassert>

using namespace std;

class A59 {
public:
    void solve(istream& in, ostream& out) {
        string s;
        in >> s;
        int lowcnt = 0, upcnt = 0, n = s.size();
        for (int i = 0; i < n; ++i) {
            lowcnt += (s[i] >= 'a' && s[i] <= 'z');
            upcnt += (s[i] >= 'A' && s[i] <= 'Z');
        }
        if (lowcnt >= upcnt) {
            for (int i = 0; i < n; ++i) {
                out << (char)tolower(s[i]);
            }
        } else {
            for (int i = 0; i < n; ++i) {
                out << (char)toupper(s[i]);
            }
        }
        out << "\n";
    }
};


int main() {
    std::ios::sync_with_stdio(false);

    A59 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
