/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>
#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>
#include <sstream>

using namespace std;

class A78 {
private:
    bool cnt_syl(const string& s, int cnt) {
        for (char c : s)
            cnt -= (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u');
        return cnt == 0;
    }
public:
    void solve(istream& in, ostream& out) {
        string s[3];
        for (int i = 0; i < 3; ++i)
            getline(in, s[i]);
        out << (cnt_syl(s[0], 5) && cnt_syl(s[1], 7) && cnt_syl(s[2], 5) ? "YES" : "NO") << "\n";
    }
};


int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);
    A78 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
