/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>

using namespace std;

class B287 {
public:
    void solve(istream& in, ostream& out) {
        long long n, k;
        in >> n >> k;
        long long mxsum = (k - 1) * (k - 1 + 1) / 2 + 1;
        if (mxsum < n) {
            out << -1 << "\n";
            return;
        }
        
        long long low = 0, high = k, ans = k;
        while (low <= high) {
            long long mid = (low + high) / 2; //mid is how many elements not to take
            long long sum = mxsum - ((mid - 1) * (mid - 1 + 1) / 2); //subtract sum 1..mid-1
            if (sum >= n) {
                ans = k - mid;
                low = mid + 1;
            } else {
                high = mid - 1;
            }
        }
        out << ans << "\n";
    }
};


int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);

    B287 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
