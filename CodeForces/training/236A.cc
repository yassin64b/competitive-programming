/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>

using namespace std;

class A236 {
public:
    void solve(istream& in, ostream& out) {
        string s;
        in >> s;
        set<char> seen;
        for (int i = 0; i < (int)s.size(); ++i) {
            seen.insert(s[i]);
        }
        out << (seen.size() % 2 ? "IGNORE HIM!\n" : "CHAT WITH HER!\n");
    }
};


int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);

    A236 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
