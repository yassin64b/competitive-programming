/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>

using namespace std;

class aoc15_1 {
public:
    void solve(istream& in, ostream& out) {
        constexpr int facA = 16807, facB = 48271;
        constexpr int MOD = 2147483647;
        long long a = 699, b = 124;
        int res = 0;
        for (int i = 0; i < 5'000'000; ++i) {
            while (a % 4) a = (a * facA) % MOD;
            while (b % 8) b = (b * facB) % MOD;
            res += ((a & 0xFFFF) == (b & 0xFFFF));
            a = (a * facA) % MOD;
            b = (b * facB) % MOD;
        }
        out << res << "\n";
    }
};


int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);

    aoc15_1 solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
