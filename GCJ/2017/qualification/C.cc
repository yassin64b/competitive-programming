/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <fstream>

#include <iostream>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <map>
#include <set>
#include <cmath>
#include <cstdlib>
#include <tuple>
#include <queue>
#include <functional>
#include <stack>
#include <numeric>
#include <cassert>

using namespace std;

class TaskC {
public:
    void solve(istream& in, ostream& out) {
        int T;
        in >> T;
        for (int t = 1; t <= T; ++t) {
            long long N, K;
            in >> N >> K;
            
            priority_queue<long long> pq; //first = len of segment
            pq.emplace(N);
            for (long long i = 0; i < K-1; ++i) {
                long long cur = pq.top();
                pq.pop();
                if (cur % 2 == 1) {
                    pq.emplace(cur / 2);
                    pq.emplace(cur / 2);
                } else {
                    pq.emplace(cur / 2 - 1);
                    pq.emplace(cur / 2);
                }
            }
            
            long long last = pq.top();
            out << "Case #" << t << ": ";
            if (last % 2 == 1) {
                out << last / 2 << " " << last / 2 << "\n";
            } else {
                out << last / 2 << " " << last / 2 - 1 << "\n";
            }
        }
    }
};

int main() {
    std::ios::sync_with_stdio(false); cin.tie(nullptr);

    TaskC solver;
    std::istream& in(cin);
    std::ostream& out(cout);
    solver.solve(in, out);
    return 0;
}
